const express=require("express"),path=require("path"),app=express(),port=process.env.PORT||10000;app.disable("x-powered-by");app.use(express.static(path.join(__dirname,"public"),{maxAge:"1h"}));app.get("/health",(_q,r)=>r.status(200).json({status:"ok",service:"moving-match-web"}));app.get("*",(_q,r)=>r.sendFile(path.join(__dirname,"public","index.html")));app.listen(port,"0.0.0.0",()=>console.log(`Moving Match web listening on ${port}`));